/**
 * Bud file for index.js
 */

'use strict'

const apeTmpl = require('ape-tmpl')

module.exports = apeTmpl.indexJsBud({
  desc: 'Class mixin generator',
  module: 'mixins',
  dirname: __dirname,
  subclasses: []
})

if (!module.parent) {
  require('coz').render(__filename)
}
