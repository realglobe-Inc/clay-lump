/**
 * Test case for mixAll.
 * Runs with mocha.
 */
'use strict'

const mixAll = require('../lib/helpers/mix_all.js')
const assert = require('assert')
const co = require('co')

describe('mix-all', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Mix all', () => co(function * () {

  }))
})

/* global describe, before, after, it */
