/**
 * Test case for mergeLumps.
 * Runs with mocha.
 */
'use strict'

const mergeLumps = require('../lib/merging/merge_lumps.js')
const assert = require('assert')
const co = require('co')

describe('merge-lumps', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Merge lumps', () => co(function * () {

  }))
})

/* global describe, before, after, it */
