/**
 * Test case for privateSheet.
 * Runs with mocha.
 */
'use strict'

const privateSheet = require('../lib/sheets/private_sheet.js')
const assert = require('assert')
const co = require('co')

describe('private-sheet', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Private sheet', () => co(function * () {

  }))
})

/* global describe, before, after, it */
