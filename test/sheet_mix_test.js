/**
 * Test case for sheetMix.
 * Runs with mocha.
 */
'use strict'

const sheetMix = require('../lib/mixins/sheet_mix.js')
const assert = require('assert')
const co = require('co')

describe('sheet-mix', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Sheet mix', () => co(function * () {

  }))
})

/* global describe, before, after, it */
