/**
 * Test case for sheet.
 * Runs with mocha.
 */
'use strict'

const sheet = require('../lib/sheets/sheet.js')
const assert = require('assert')
const co = require('co')

describe('sheet', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Sheet', () => co(function * () {

  }))
})

/* global describe, before, after, it */
